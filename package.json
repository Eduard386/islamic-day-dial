{
  "name": "islamic-day-dial",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run test && npm run test:ios && npm run build --workspaces --if-present",
    "build:demo": "npm run build:demo --workspace=@islamic-day-dial/web-dashboard",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ios": "./scripts/run-ios-tests.sh",
    "lint": "eslint 'packages/*/src/**/*.ts'",
    "typecheck": "tsc --build",
    "precommit": "npm run lint && npm run typecheck && npm test && npm run test:ios",
    "test:wear": "cd apps/wear-watchface && ./gradlew testDebugUnitTest",
    "prepare": "husky"
  },
  "devDependencies": {
    "eslint": "^10.0.3",
    "husky": "^9.1.7",
    "sharp": "^0.34.5",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.57.1",
    "vitest": "^3.0.0"
  }
}
